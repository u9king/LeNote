%%数字、字符与字符串、矩阵、元胞数组、结构体
%字符与字符串
s = 'a';
abs(s)  %取ASCII码
char(65) %ASCII码转字符
num2str(65) %数字转字符串
doc num2str %查询函数作用
%矩阵
A = [1 2 3;4 5 2;3 2 7];
B = A'; %转置
C = A(:);  %拉伸为列向量
D = inv(A); %逆矩阵

E = zero(10,5,3); %10行5列3通道全0矩阵
E(:,:,1) = rand(10,5);
E(:,:,2) = randi(5,10,5);
E(:,:,3) = randn(10,5);